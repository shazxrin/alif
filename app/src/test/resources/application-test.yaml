spring:
  application:
    name: alif
  datasource:
    url: jdbc:postgresql://localhost:5432/alif
    username: user
    password: password
  rabbitmq:
    host: localhost
    username: user
    password: password

